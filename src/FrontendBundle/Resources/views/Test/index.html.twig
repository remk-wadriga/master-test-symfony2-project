{% extends 'FrontendBundle::main.html.twig' %}

{% block scriptFiles %}
    {{ parent() }}
    <script src="{{ asset('bundles/frontend/js/pages/test.js') }}" type="text/javascript"></script>
{% endblock %}

{% block content %}

    <h1>{{ test.name }}</h1>
    <br />
    <h3><a href="{{ path('edit_test_type', {'id': test.id} ) }}">{{ 'Редактировать' | trans }}</a></h3>

{% endblock %}

{% block scripts %}
    {{ parent() }}
    Test.init();
{% endblock %}